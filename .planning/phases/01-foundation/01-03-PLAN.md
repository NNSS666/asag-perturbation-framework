---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/asag/infra/__init__.py
  - src/asag/infra/run_dir.py
  - src/asag/infra/config.py
  - src/asag/infra/versions.py
  - src/asag/infra/seeds.py
  - src/asag/infra/storage.py
autonomous: true
requirements:
  - DATA-03
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "All intermediate outputs (perturbations, grades, metrics) can be written to and read from JSON files without information loss — Pydantic roundtrip is lossless"
    - "Every experiment run creates a uniquely-named directory (timestamp + corpus + seed) containing a config.json with full experiment configuration, library versions, platform info, and seed values"
    - "Per-component seeds (split, perturbation, training) are independently settable — changing one does not affect the others"
    - "Library versions for all tracked packages are captured at run start and written to config.json"
  artifacts:
    - path: "src/asag/infra/storage.py"
      provides: "JSON-based storage for Pydantic records — save and load functions for any record type"
      contains: "def save_records"
    - path: "src/asag/infra/run_dir.py"
      provides: "Auto-named run directory creation with timestamp + corpus + seed"
      contains: "def make_run_dir"
    - path: "src/asag/infra/versions.py"
      provides: "Library version capture using importlib.metadata"
      contains: "def get_library_versions"
    - path: "src/asag/infra/seeds.py"
      provides: "Per-component seed management with isolation between split, perturbation, and training seeds"
      contains: "class SeedConfig"
    - path: "src/asag/infra/config.py"
      provides: "Experiment configuration dataclass with serialization"
      contains: "class ExperimentConfig"
  key_links:
    - from: "src/asag/infra/storage.py"
      to: "src/asag/schema/records.py"
      via: "uses Pydantic model_dump_json() and model_validate_json() for type-safe roundtrip"
      pattern: "model_dump_json|model_validate_json"
    - from: "src/asag/infra/config.py"
      to: "src/asag/infra/versions.py"
      via: "calls get_library_versions() when saving config"
      pattern: "get_library_versions"
    - from: "src/asag/infra/config.py"
      to: "src/asag/infra/seeds.py"
      via: "includes SeedConfig in experiment configuration"
      pattern: "SeedConfig"
---

<objective>
Build the reproducibility infrastructure: JSON-based storage for all intermediate outputs, auto-named run directories, library version capture, per-component seed management, and experiment configuration logging.

Purpose: Every experiment must be fully reproducible. This infrastructure ensures that all outputs are persisted in a structured format, every run is traceable to its exact configuration, and seeds are isolated so multi-seed experiments can vary one component while holding others fixed.

Output: Complete infra module with storage, run directory creation, version capture, seed management, and experiment config — all exercised by self-tests.
</objective>

<execution_context>
@/Users/nando/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nando/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement JSON storage and seed management</name>
  <files>
    src/asag/infra/__init__.py
    src/asag/infra/storage.py
    src/asag/infra/seeds.py
  </files>
  <action>
**`src/asag/infra/storage.py` — JSON storage (DATA-03):**
- Define `save_records(records: List[BaseModel], path: Path) -> None`:
  - Write Pydantic records as JSONL (one JSON object per line)
  - Use `record.model_dump_json()` for each record
  - Create parent directories if they don't exist
- Define `load_records(path: Path, model_class: Type[T]) -> List[T]`:
  - Read JSONL back, use `model_class.model_validate_json(line.strip())` for each line
  - Return typed list of Pydantic model instances
- Define `save_json(data: dict, path: Path) -> None`:
  - Write arbitrary dict as pretty-printed JSON (for config files)
  - Use `json.dump(data, f, indent=2, default=str)` to handle datetime, Path, etc.
- Define `load_json(path: Path) -> dict`:
  - Read JSON file back as dict
- Add `if __name__ == "__main__"` roundtrip test: create 3 QuestionRecords, save to temp file, load back, assert equality

**`src/asag/infra/seeds.py` — Per-component seed management (INFR-01):**
- Define `class SeedConfig(BaseModel)`:
  - `split_seed: int = 42` — controls Protocol A/B splitting
  - `perturb_seed: int = 42` — controls perturbation generation
  - `train_seed: int = 42` — controls model training initialization
  - All default to 42 but independently variable for multi-seed experiments
- Define `set_global_seeds(seed: int) -> None`:
  - Sets `random.seed(seed)`, `numpy.random.seed(seed)`, and if torch is available, `torch.manual_seed(seed)` + `torch.cuda.manual_seed_all(seed)`
  - Wrap torch calls in try/except ImportError (torch not installed in Phase 1)
- Define `set_split_seeds(seed: int) -> None`:
  - Sets only `random.seed(seed)` and `numpy.random.seed(seed)` — used before splitter calls
- Add docstring explaining seed isolation philosophy: each component gets its own seed passed via random_state parameter; global seeds are set only as a fallback for libraries that don't accept random_state

IMPORTANT: SeedConfig uses Pydantic BaseModel (NOT frozen — seeds may be varied). Use `from typing import Optional` — Python 3.9 compatible.
  </action>
  <verify>
Run `PYTHONPATH=src python -c "
from asag.infra.storage import save_records, load_records
from asag.schema.records import QuestionRecord
from pathlib import Path
import tempfile, os
qs = [
    QuestionRecord(question_id='q1', prompt='What is X?', reference_answers=['X is Y']),
    QuestionRecord(question_id='q2', prompt='What is Z?', reference_answers=['Z is W', 'Z is also V']),
]
with tempfile.TemporaryDirectory() as td:
    p = Path(td) / 'test.jsonl'
    save_records(qs, p)
    loaded = load_records(p, QuestionRecord)
    assert len(loaded) == 2
    assert loaded[0] == qs[0]
    assert loaded[1].reference_answers == ['Z is W', 'Z is also V']
    print('Storage roundtrip OK')
"`.
Run `PYTHONPATH=src python -c "
from asag.infra.seeds import SeedConfig, set_global_seeds
sc = SeedConfig(split_seed=42, perturb_seed=123, train_seed=456)
assert sc.split_seed == 42
assert sc.perturb_seed == 123
set_global_seeds(42)
print('Seeds OK')
"`.
  </verify>
  <done>
JSON storage saves and loads Pydantic records losslessly via JSONL format. SeedConfig allows independent seed values for split, perturbation, and training components with defaults of 42. set_global_seeds and set_split_seeds correctly set random and numpy seeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement run directory creation, version capture, and experiment config</name>
  <files>
    src/asag/infra/run_dir.py
    src/asag/infra/versions.py
    src/asag/infra/config.py
  </files>
  <action>
**`src/asag/infra/versions.py` — Library version capture (INFR-02):**
- Define `TRACKED_PACKAGES: List[str] = ["pydantic", "datasets", "scikit-learn", "numpy", "torch", "transformers", "sentence-transformers", "openai"]` — covers all phases' dependencies
- Define `get_library_versions() -> Dict[str, str]`:
  - Start with `{"python": sys.version, "platform": platform.platform()}`
  - For each tracked package, try `importlib.metadata.version(pkg)`, catch PackageNotFoundError and record "not_installed"
  - Return the dict
- Add `if __name__ == "__main__"` block that prints all versions

**`src/asag/infra/run_dir.py` — Run directory creation (INFR-02):**
- Define `make_run_dir(base: Path, corpus: str, seed: int) -> Path`:
  - Generate timestamp: `datetime.now().strftime("%Y-%m-%d_%H-%M")`
  - Build run name: `f"{timestamp}_{corpus}_seed{seed}"`
  - Create `base / run_name` directory with `mkdir(parents=True, exist_ok=False)` — fail if exists to prevent overwriting
  - Return the created Path
- Default `base` should be `Path("runs")` relative to project root
- Add docstring explaining naming convention per CONTEXT.md: `runs/2026-02-20_14-30_beetle_seed42/`

**`src/asag/infra/config.py` — Experiment configuration (INFR-02):**
- Define `class ExperimentConfig(BaseModel)`:
  - `corpus: str` — dataset name
  - `seeds: SeedConfig` — per-component seeds (imports from seeds.py)
  - `protocol: Literal["A", "B", "both"] = "both"` — which evaluation protocol(s) to run
  - `description: Optional[str] = None` — free-text experiment description
  - `extra: Dict[str, Any] = {}` — catch-all for phase-specific hyperparameters (model name, learning rate, etc.)
- Define `save_run_config(run_dir: Path, config: ExperimentConfig) -> Path`:
  - Build config record dict: `{"config": config.model_dump(), "library_versions": get_library_versions(), "timestamp": datetime.now().isoformat()}`
  - Write to `run_dir / "config.json"` using `save_json` from storage.py
  - Return the config.json path
- Define `load_run_config(run_dir: Path) -> Dict`:
  - Read `run_dir / "config.json"` using `load_json` from storage.py
  - Return the dict (not an ExperimentConfig — config.json may have extra fields from version info)
- Add `if __name__ == "__main__"` block that creates a temp run directory, saves a config, loads it back, and prints it

**`src/asag/infra/__init__.py`:**
- Re-export: `save_records`, `load_records`, `save_json`, `load_json`, `SeedConfig`, `set_global_seeds`, `set_split_seeds`, `make_run_dir`, `ExperimentConfig`, `save_run_config`, `load_run_config`, `get_library_versions`

IMPORTANT: Use `from typing import Dict, List, Any, Optional, Literal` — Python 3.9 compatible. Use `from datetime import datetime` (not `datetime.datetime`).
  </action>
  <verify>
Run `PYTHONPATH=src python -c "
from asag.infra.versions import get_library_versions
versions = get_library_versions()
assert 'python' in versions
assert 'pydantic' in versions
print(f'Tracked {len(versions)} packages')
for k, v in versions.items():
    print(f'  {k}: {v}')
"`.
Run `PYTHONPATH=src python -c "
from asag.infra.run_dir import make_run_dir
from asag.infra.config import ExperimentConfig, save_run_config, load_run_config
from asag.infra.seeds import SeedConfig
from pathlib import Path
import tempfile
with tempfile.TemporaryDirectory() as td:
    run_dir = make_run_dir(Path(td), 'beetle', 42)
    assert run_dir.exists()
    assert 'beetle' in run_dir.name
    assert 'seed42' in run_dir.name
    config = ExperimentConfig(corpus='beetle', seeds=SeedConfig())
    config_path = save_run_config(run_dir, config)
    assert config_path.exists()
    loaded = load_run_config(run_dir)
    assert loaded['config']['corpus'] == 'beetle'
    assert 'library_versions' in loaded
    print(f'Run dir: {run_dir.name}')
    print(f'Config saved and loaded OK')
"`.
  </verify>
  <done>
Run directories are auto-named with timestamp + corpus + seed. Library versions for all tracked packages are captured at run start. ExperimentConfig serializes the full experiment setup (corpus, seeds, protocol, extra params) to config.json alongside version info. Config can be loaded back for audit. All infra modules are re-exported from asag.infra.
  </done>
</task>

</tasks>

<verification>
1. `save_records` + `load_records` performs lossless JSONL roundtrip for QuestionRecord, AnswerRecord, and PerturbationRecord
2. `make_run_dir` creates a uniquely-named directory following the `YYYY-MM-DD_HH-MM_corpus_seedN` convention
3. `save_run_config` writes config.json with experiment config + library versions + timestamp
4. `SeedConfig` allows independent seed values for split, perturbation, and training
5. `get_library_versions` captures Python version and all tracked package versions
</verification>

<success_criteria>
- JSON storage handles all three Pydantic record types with lossless roundtrip
- Run directory naming follows CONTEXT.md convention (timestamp + corpus + seed)
- Library versions are captured using importlib.metadata (Python 3.9 compatible)
- Per-component seeds are independently settable with SeedConfig
- ExperimentConfig captures full run configuration for reproducibility
- All code is Python 3.9 compatible
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
